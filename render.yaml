services:
  - type: web
    name: gpt-oss-api
    runtime: docker
    dockerfilePath: Dockerfile
    envVars:
      - key: PORT
        value: 8000
      - key: INFERENCE_BACKEND
        value: transformers
      - key: CHECKPOINT_PATH
        value: microsoft/DialoGPT-small
    plan: starter
